```mermaid
C4Context
    title mmdx 系统上下文
    Person(agent, "AI Agent", "写文档、生成图表")
    System(mmdx, "mmdx CLI", "Mermaid 渲染导出")
    System_Ext(browser, "Chromium/Edge", "渲染内核")
    Rel(agent, mmdx, "调用")
    Rel(mmdx, browser, "驱动")
```
